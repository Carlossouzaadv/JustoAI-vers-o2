# ====================================================================
# JUSTOAI V2 - VERCEL FRONTEND ENVIRONMENT VARIABLES
# ====================================================================
# Este arquivo contém APENAS as variáveis necessárias para o frontend
# rodando no Vercel (Pages, Components, UI)
#
# IMPORTANTE: O backend roda no Railway, então a maioria das variáveis
# NÃO são necessárias aqui. Apenas variáveis públicas (NEXT_PUBLIC_*)
# ====================================================================

# --------------------------------------------------------------------
# PUBLIC VARIABLES (Embedded in client bundle)
# --------------------------------------------------------------------

# URL da aplicação (seu domínio customizado ou Vercel URL)
NEXT_PUBLIC_APP_URL=https://justoai-v2.vercel.app

# URL do backend Railway (CRITICAL - todas as API calls vão para aqui)
NEXT_PUBLIC_API_URL=https://justoai-vers-o2-production.up.railway.app

# Supabase (para auth no frontend)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# --------------------------------------------------------------------
# FEATURE FLAGS (Public)
# --------------------------------------------------------------------
NEXT_PUBLIC_CLASSIC_DASHBOARD_ENABLED=true
NEXT_PUBLIC_PRO_FEATURES_ENABLED=true
NEXT_PUBLIC_PROCESS_MONITORING_ENABLED=true
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_ENABLE_DEBUG=false
NEXT_PUBLIC_SWAGGER_ENABLED=false
NEXT_PUBLIC_UPLOAD_MAX_SIZE=10485760

# ====================================================================
# THAT'S IT! 🎉
# ====================================================================
# O frontend não precisa de mais nada.
# Todas as variáveis de backend (DATABASE_URL, REDIS, SMTP, etc)
# estão configuradas no Railway.
#
# O frontend apenas:
# 1. Chama APIs no Railway via NEXT_PUBLIC_API_URL
# 2. Usa Supabase para autenticação
# 3. Renderiza páginas e componentes
# ====================================================================

# ====================================================================
# COMO CONFIGURAR NO VERCEL
# ====================================================================
# 1. Acesse: https://vercel.com/[team]/justoai-v2/settings/environment-variables
# 2. Clique em "Add New"
# 3. Cole cada variável acima
# 4. Selecione environment: Production (e Preview se quiser)
# 5. Clique "Save"
#
# OU use o Vercel CLI:
# vercel env add NEXT_PUBLIC_APP_URL production
# vercel env add NEXT_PUBLIC_API_URL production
# ... etc
# ====================================================================

# ====================================================================
# CHECKLIST VERCEL
# ====================================================================
# ✅ NEXT_PUBLIC_APP_URL (seu domínio)
# ✅ NEXT_PUBLIC_API_URL (URL do Railway) ⭐ CRITICAL
# ✅ NEXT_PUBLIC_SUPABASE_URL
# ✅ NEXT_PUBLIC_SUPABASE_ANON_KEY
# ✅ Feature flags (todos NEXT_PUBLIC_*)
# ====================================================================
